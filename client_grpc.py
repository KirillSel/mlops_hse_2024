import grpc

# Импортируем сгенерированные файлы после создания protobuf (позже)
# import mlops_service.grpc.service_pb2 as service_pb2
# import mlops_service.grpc.service_pb2_grpc as service_pb2_grpc

# Основная логика gRPC клиента будет добавлена здесь
def run():
    print("gRPC client running")

if __name__ == "__main__":
    run()
